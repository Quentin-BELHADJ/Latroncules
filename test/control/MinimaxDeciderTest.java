package control;

import boardifier.control.StageFactory;
import boardifier.model.Model;
import boardifier.view.View;
import model.LatronculesBoard;
import model.LatronculesStageModel;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;

class MinimaxDeciderTest {

/*
    LatronculesStageModel latronculesStageModel;

    @BeforeAll
    static void setUp() {
        StageFactory.registerModelAndView("Latroncules.java", "model.LatronculesStageModel.java", "view.LatronculesStageView.java");
        Model model = new Model();
        View view = new View(model);
        LatronculesController control = new LatronculesController(model,view);
        MinimaxDecider minimaxDecider = new MinimaxDecider(model,control);
    }

    @Test
    void decide() {
    }

    @Test
    void evaluation() {
        LatronculesBoard board = new LatronculesBoard(0,0,latronculesStageModel);
        board.loadFromString("");
    }

    @Test
    void minimax() {
    }

    @Test
    void getPossibilities() {

    }


 */
}